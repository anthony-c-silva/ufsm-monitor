apiVersion: 1

datasources:
  - name: TimescaleDB
    uid: timescaledb
    type: postgres
    access: proxy
    url: timescaledb:5432
    user: ufsm
    isDefault: true
    jsonData:
      database: monitor
      sslmode: disable
      postgresVersion: 1600
      timescaledb: true
    secureJsonData:
      password: ufsm
